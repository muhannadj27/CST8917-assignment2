# Azure Functions local dev
local.settings.json
bin/
obj/
__pycache__/
*.pyc
.python_packages/
.venv/
venv/

# Logic Apps generated connections (contains secrets)
version-b-logic-apps/logicapp/connections.json

# Live-demo scratch files with a real function key baked in — for local
# recording use only, never committed
presentation/local-demo/

# OS / editor
.DS_Store
.vscode/
*.swp
